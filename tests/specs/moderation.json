[
  {
    "name": "group setup exposes the moderation controls",
    "steps": [
      { "send": { "callback": "setup:open" }, "expect": [{ "method": "sendMessage", "payload": { "text": "Choose what you’d like to configure." } }] },
      { "send": { "callback": "setup:words" }, "expect": [{ "method": "editMessageText", "payload": { "text": "Send blocked words separated by commas. Send ‘clear’ to remove them." } }] },
      { "send": { "text": "spam" }, "expect": [{ "method": "sendMessage", "payload": { "text": "Blocked words saved." } }] }
    ]
  }
]
